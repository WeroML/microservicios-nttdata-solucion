# La configuración de la aplicación vive en tacocloud/src/main/resources/application.yml
# (el runtime). Este archivo queda vacío a propósito: dentro del JAR del runtime
# sería ignorado y sólo generaría confusión.
